startprogram
var integer numOne, numOmf, numTwo, numThree, max, min;

numOne = 10;
numTwo = 55;
numThree = 12;


if numOne >= numTwo and numOne >= numThree then
    max = numOne;
else
    if numTwo >= numOne and numTwo >= numThree then
        max = numTwo;
    else
        max = numThree;
    endif
endif

if numOne <= numTwo and numOne <= numThree then
    min = numOne;
else
    if numTwo <= numOne and numTwo <= numThree then
        min = numTwo;
    else
        min = numThree;
    endif
endif


write max;
write min;
endprogram
